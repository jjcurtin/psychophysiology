filename = ['P:\UW\Users\Kaye\stroop matlab\trial1']


y = wavread(filename)
%[y, Fs] = wavread(filename)
%[y, Fs, nbits] = wavread(filename)
%[y, Fs, nbits, opts] = wavread(filename)
%[...] = wavread(filename, 1)
%[...] = wavread(filename, [N1 N2])
%[...] = wavread(..., fmt)
%siz = wavread(filename,'size')